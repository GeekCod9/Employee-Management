import React from 'react';
import './navbar.css'

const Navbar = (props) => {
    return(
        <div className="Navbar">
            <a href="#">Events</a>
            <a href="#">Schedule Events</a>

        </div>
    )
}

export default Navbar